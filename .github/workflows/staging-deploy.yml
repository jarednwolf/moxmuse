name: Staging Environment Deployment

on:
  push:
    branches: [develop, staging]
  pull_request:
    branches: [main]

env:
  NODE_VERSION: '20'
  PNPM_VERSION: '8.15.8'

jobs:
  # Deploy to staging for feature branches and PRs
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    environment: staging
    timeout-minutes: 15

    steps:
      - name: Check required secrets
        id: check-secrets
        run: |
          if [ -z "${{ secrets.VERCEL_TOKEN }}" ] || [ -z "${{ secrets.VERCEL_ORG_ID }}" ] || [ -z "${{ secrets.VERCEL_PROJECT_ID }}" ]; then
            echo "missing=true" >> $GITHUB_OUTPUT
          else
            echo "missing=false" >> $GITHUB_OUTPUT
          fi

      - name: Checkout code
        if: steps.check-secrets.outputs.missing == 'false'
        uses: actions/checkout@v4

      - name: Setup Node.js
        if: steps.check-secrets.outputs.missing == 'false'
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Enable Corepack and activate pnpm
        if: steps.check-secrets.outputs.missing == 'false'
        run: |
          corepack enable
          corepack prepare pnpm@${{ env.PNPM_VERSION }} --activate

      - name: Enable Corepack and activate pnpm
        if: steps.check-secrets.outputs.missing == 'false'
        run: |
          corepack enable
          corepack prepare pnpm@${{ env.PNPM_VERSION }} --activate

      - name: Verify pnpm
        if: steps.check-secrets.outputs.missing == 'false'
        run: pnpm --version

      - name: Install dependencies
        if: steps.check-secrets.outputs.missing == 'false'
        run: pnpm install --no-frozen-lockfile

      - name: Build application
        if: steps.check-secrets.outputs.missing == 'false'
        run: pnpm build

      - name: Deploy to Vercel Preview
        if: steps.check-secrets.outputs.missing == 'false'
        uses: amondnet/vercel-action@v25
        id: vercel-preview
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          github-comment: true
          vercel-args: '--env DATABASE_URL="${{ secrets.STAGING_DATABASE_URL }}" --env OPENAI_API_KEY="${{ secrets.OPENAI_API_KEY }}" --env NEXTAUTH_SECRET="${{ secrets.STAGING_NEXTAUTH_SECRET }}"'

      - name: Run staging database migrations
        if: steps.check-secrets.outputs.missing == 'false'
        run: |
          pnpm db:migrate:deploy
        env:
          DATABASE_URL: ${{ secrets.STAGING_DATABASE_URL }}

      - name: Health check staging deployment
        if: steps.check-secrets.outputs.missing == 'false' && steps.vercel-preview.outputs.preview-url != ''
        run: |
          sleep 30
          curl -f ${{ steps.vercel-preview.outputs.preview-url }}/api/health || exit 1

      - name: Run smoke tests
        if: steps.check-secrets.outputs.missing == 'false' && steps.vercel-preview.outputs.preview-url != ''
        run: pnpm test:smoke --baseURL=${{ steps.vercel-preview.outputs.preview-url }}

      - name: Comment PR with staging URL
        if: steps.check-secrets.outputs.missing == 'false' && github.event_name == 'pull_request' && steps.vercel-preview.outputs.preview-url != ''
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `🚀 Staging deployment ready!
              
              **Preview URL:** ${{ steps.vercel-preview.outputs.preview-url }}
              
              **Test the following:**
              - [ ] AI deck generation workflow
              - [ ] Mobile responsiveness
              - [ ] Performance and loading times
              - [ ] Error handling
              
              **Automated checks:**
              - ✅ Health checks passed
              - ✅ Smoke tests passed
              `
            })

      - name: Skip Notice (secrets missing)
        if: steps.check-secrets.outputs.missing == 'true'
        run: |
          echo "Vercel secrets missing; skipping staging deploy steps. Marking job as success to avoid PR failures."

  # Cleanup old staging deployments
  cleanup-staging:
    name: Cleanup Old Staging Deployments
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'

    steps:
      - name: Cleanup old deployments
        run: |
          # Keep only the last 5 staging deployments
          curl -H "Authorization: Bearer ${{ secrets.VERCEL_TOKEN }}" \
            "https://api.vercel.com/v6/deployments?projectId=${{ secrets.VERCEL_PROJECT_ID }}&limit=20" \
            | jq -r '.deployments[] | select(.target != "production") | .uid' \
            | tail -n +6 \
            | xargs -I {} curl -X DELETE \
              -H "Authorization: Bearer ${{ secrets.VERCEL_TOKEN }}" \
              "https://api.vercel.com/v13/deployments/{}"